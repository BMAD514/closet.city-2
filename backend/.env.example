# Copy this file to .env and populate with your credentials for local development.
GEMINI_API_KEY=
# PostgreSQL connection string, e.g. postgres://user:password@localhost:5432/closet_city
DATABASE_URL=
# Comma-separated list of allowed origins for CORS. Use * to allow all origins.
ALLOWED_ORIGINS=http://localhost:5173
# Optional: override the port the gateway listens on (defaults to 4000)
# PORT=4000
# Optional: override the JWT secret used for signing auth tokens
# JWT_SECRET=
